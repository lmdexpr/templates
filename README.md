# My templates

## flake
Basic nix project

```sh 
nix flake init -t 'github:lmdexpr/templates#flake'
```

## opam
OCaml project.

```sh 
nix flake init -t 'github:lmdexpr/templates#opam'
```

## OCaml API server and Rescript SPA

```sh 
nix flake init -t 'github:lmdexpr/templates#fullstack'
```
